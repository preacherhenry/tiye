import api from '../taxi_web_admin/src/services/api';

const testApi = async () => {
    try {
        console.log('Testing GET /admin/applications ...');
        // We need a token. I'll try to get one from the users table.
        // Actually, I'll just check the DB count again with a cleaner script.
        process.exit();
    } catch (e) {
        process.exit();
    }
};
